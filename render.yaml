services:
  - type: web
    name: translation-backend
    env: php
    rootDir: Translation-Backend
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      php artisan key:generate
      php artisan config:cache
    startCommand: php artisan serve --host 0.0.0.0 --port $PORT
